import type { ExtractionResult, ExtractionConfig } from '../types';
/**
 * Extract energy data from document text.
 */
export declare function extractEnergy(text: string, config?: ExtractionConfig): ExtractionResult;
